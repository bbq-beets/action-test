name: Test

on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout Repo'
        uses: actions/checkout@v2

      - name: cat
        run: cat test.json

      - name: swapfile
        run: sudo swapoff /mnt/swapfile;sudo mkswap /mnt/swapfile;sudo swapon /mnt/swapfile
